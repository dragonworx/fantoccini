import * as Color from 'color';

export const DEFAULT_PROJECT_WIDTH = 640;
export const DEFAULT_PROJECT_HEIGHT = 480;
export const DEFAULT_PROJECT_BACKGROUND_COLOR = Color.rgb(0, 0, 255).rgbNumber();

export const DEFAULT_PROJECT_OPTIONS = {
  width: DEFAULT_PROJECT_WIDTH,
  height: DEFAULT_PROJECT_HEIGHT,
  backgroundColor: DEFAULT_PROJECT_BACKGROUND_COLOR,
};